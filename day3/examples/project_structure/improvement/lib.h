#pragma once

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

/*
 * We're calculating the 'N'th factorial
 */ 
extern int N;


/*
 * Function declarations
 */ 
int recursive_factorial(int n);
int iterative_factorial(int n);
double sqrt_of_factorial(int n);

